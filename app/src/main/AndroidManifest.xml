<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.serasiautoraya.slimobiledrivertracking">

    <application
        android:name=".util.AppInit"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MVP.Login.LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MVP.Dashboard.DashboardActivity" android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".MVP.JourneyOrder.Activity.ActivityDetailActivity"/>

        <activity android:name=".activity.DashboardActivity" android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".activity.EvidenceCaptureActivity"/>
        <activity android:name=".activity.ExpanseCaptureActivity"/>
        <activity android:name=".activity.OrderDetailActivity"/>
        <activity android:name=".activity.ChangePasswordActivity"/>
        <activity android:name=".activity.FatigueInterviewActivity"/>
        <activity android:name=".activity.ProveActivity"/>
        <activity android:name=".activity.SignatureActivity"/>
        <activity android:name=".activity.ProfileActivity"/>
        <activity android:name=".activity.ActionActivityActivity"/>


        <service android:name=".util.GPSTracker"
            android:enabled="true"
            android:exported="false"
            android:label="LocationService"
        />

        <!--<service android:name=".util.FCMMessageService">-->
            <!--<intent-filter>-->
                <!--<action android:name="com.google.firebase.MESSAGING_EVENT"/>-->
            <!--</intent-filter>-->
        <!--</service>-->

        <service android:name=".util.FirebaseInstanceIdServiceUtil">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT"/>
            </intent-filter>
        </service>

        <!--<receiver android:name=".util.FCMMessageRestartServiceReceiver" >-->
            <!--<intent-filter>-->
                <!--<action android:name="FCMReceiver" >-->
                <!--</action>-->
            <!--</intent-filter>-->
        <!--</receiver>-->

        <service
            android:name=".util.GPSTrackerService"
            android:exported="false"></service>
        <receiver android:name=".util.GPSTrackerServiceReceiver"></receiver>

    </application>

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET" />

</manifest>